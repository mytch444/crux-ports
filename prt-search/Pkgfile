# Description: Similar to prt-get search but also prints the repo with the port.
# Maintainer: Mytchel Hammond, mytchel at openmailbox dot org

name=prt-search
version=3
release=1
source=($name)

build() {
	install -m 0755 -D $SRC/$name $PKG/usr/bin/$name
}
